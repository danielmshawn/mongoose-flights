const express = require('express');
const router = express.Router();
const ticketsCtrl = require('../controllers/tickets')

//NOt sure  waht this should be
// router.get(`/flights/:id/tickets`, ticketsCtrl.create);


module.exports = router